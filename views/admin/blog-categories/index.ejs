<%- include('../../adminlayout/header') %>

<div class="page-wrapper">

    <!-- Start Content -->
    <div class="content pb-0">

        <!-- =========================
             ALERTS
        ========================== -->

        <% if (typeof success !== 'undefined' && success) { %>
            <div class="alert alert-success alert-dismissible fade show mb-4" role="alert">
                <%= success %>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        <% } %>

        <% if (typeof errors !== 'undefined' && errors && errors.length) { %>
            <div class="alert alert-danger alert-dismissible fade show mb-4" role="alert">
                <ul class="mb-0">
                    <% errors.forEach(function(error) { %>
                        <li><%= error %></li>
                    <% }) %>
                </ul>
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        <% } %>

        <!-- =========================
             PAGE HEADER
        ========================== -->

        <div class="d-flex align-items-center justify-content-between gap-2 mb-4 flex-wrap">

            <div>
                <h4 class="mb-1">Blog Categories</h4>
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb mb-0 p-0">
                        <li class="breadcrumb-item">
                            <a href="/admin">Home</a>
                        </li>
                        <li class="breadcrumb-item active" aria-current="page">
                            Blog Categories
                        </li>
                    </ol>
                </nav>
            </div>

            <div class="gap-2 d-flex align-items-center flex-wrap">

                <!-- Export -->
                <div class="dropdown">
                    <a href="javascript:void(0);" class="dropdown-toggle btn btn-outline-light px-2 shadow" data-bs-toggle="dropdown">
                        <i class="ti ti-package-export me-2"></i> Export
                    </a>
                    <div class="dropdown-menu dropdown-menu-end">
                        <ul>
                            <li>
                                <a href="/admin/blog-categories/export/pdf" class="dropdown-item">
                                    <i class="ti ti-file-type-pdf me-1"></i> Export as PDF
                                </a>
                            </li>
                            <li>
                                <a href="/admin/blog-categories/export/excel" class="dropdown-item">
                                    <i class="ti ti-file-type-xls me-1"></i> Export as Excel
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>

                <!-- Refresh -->
                <a href="javascript:location.reload();" class="btn btn-icon btn-outline-light shadow" data-bs-toggle="tooltip" data-bs-placement="top" aria-label="Refresh" data-bs-original-title="Refresh">
                    <i class="ti ti-refresh"></i>
                </a>

                <!-- Collapse -->
                <a href="javascript:void(0);" class="btn btn-icon btn-outline-light shadow" data-bs-toggle="tooltip" data-bs-placement="top" aria-label="Collapse" data-bs-original-title="Collapse" id="collapse-header">
                    <i class="ti ti-transition-top"></i>
                </a>

            </div>

        </div>

        <!-- =========================
             BLOG CATEGORIES CARD
        ========================== -->

        <div class="card border-0 rounded-0">

            <!-- CARD HEADER -->
            <div class="card-header d-flex align-items-center justify-content-between gap-2 flex-wrap">

                <!-- Search -->
                <form method="GET" action="/admin/blog-categories" class="mb-0">
                    <div class="input-icon input-icon-start position-relative">
                        <span class="input-icon-addon text-dark">
                            <i class="ti ti-search"></i>
                        </span>
                        <input type="text" name="search" value="<%= typeof search !== 'undefined' ? search : '' %>" class="form-control" id="categorySearch" placeholder="Search Categories...">
                    </div>
                </form>

                <!-- Add Category -->
                <div class="d-inline-flex align-items-center gap-3">
                    <a href="/admin/blog-categories/create" class="btn btn-primary">
                        <i class="ti ti-square-rounded-plus-filled me-1"></i> Add Category
                    </a>
                </div>

            </div>

            <!-- =========================
                 CARD BODY
            ========================== -->

            <div class="card-body">

                <!-- =========================
                     TABLE HEADER & TOOLS
                ========================== -->

                <div class="d-flex align-items-center justify-content-between flex-wrap gap-2 mb-3">

                    <!-- SORT -->
                    <div class="d-flex align-items-center gap-2 flex-wrap">
                        <div class="dropdown">
                            <a href="javascript:void(0);" class="dropdown-toggle btn btn-outline-light shadow" data-bs-toggle="dropdown">
                                <i class="ti ti-sort-ascending-2 me-2"></i> Sort By
                            </a>
                            <div class="dropdown-menu">
                                <ul>
                                    <li>
                                        <a href="/admin/blog-categories?sort=name_asc&search=<%= encodeURIComponent(typeof search !== 'undefined' ? search : '') %>" class="dropdown-item">
                                            Name (A-Z)
                                        </a>
                                    </li>
                                    <li>
                                        <a href="/admin/blog-categories?sort=name_desc&search=<%= encodeURIComponent(typeof search !== 'undefined' ? search : '') %>" class="dropdown-item">
                                            Name (Z-A)
                                        </a>
                                    </li>
                                    <li>
                                        <a href="/admin/blog-categories?sort=order_asc&search=<%= encodeURIComponent(typeof search !== 'undefined' ? search : '') %>" class="dropdown-item">
                                            Order (Low to High)
                                        </a>
                                    </li>
                                    <li>
                                        <a href="/admin/blog-categories?sort=order_desc&search=<%= encodeURIComponent(typeof search !== 'undefined' ? search : '') %>" class="dropdown-item">
                                            Order (High to Low)
                                        </a>
                                    </li>
                                </ul>
                            </div>
                        </div>
                    </div>

                    <!-- RIGHT TOOLS -->
                    <div class="d-flex align-items-center gap-2 flex-wrap">

                        <!-- Manage Columns Dropdown -->
                        <div class="dropdown">
                            <a href="javascript:void(0);" class="btn bg-soft-indigo border-0" data-bs-toggle="dropdown" data-bs-auto-close="outside">
                                <i class="ti ti-columns-3 me-2"></i> Manage Columns
                            </a>

                            <div class="dropdown-menu dropdown-menu-md dropdown-md p-3">
                                <ul class="list-unstyled mb-0">

                                    <!-- ID Column -->
                                    <li class="gap-1 d-flex align-items-center mb-2">
                                        <i class="ti ti-columns me-1"></i>
                                        <div class="form-check form-switch w-100 ps-0">
                                            <label class="form-check-label d-flex align-items-center gap-2 w-100">
                                                <span># ID</span>
                                                <input class="form-check-input column-toggle ms-auto" type="checkbox" data-column="0" checked>
                                            </label>
                                        </div>
                                    </li>

                                    <!-- Category Name Column -->
                                    <li class="gap-1 d-flex align-items-center mb-2">
                                        <i class="ti ti-columns me-1"></i>
                                        <div class="form-check form-switch w-100 ps-0">
                                            <label class="form-check-label d-flex align-items-center gap-2 w-100">
                                                <span>Category Name</span>
                                                <input class="form-check-input column-toggle ms-auto" type="checkbox" data-column="1" checked>
                                            </label>
                                        </div>
                                    </li>

                                    <!-- Slug Column -->
                                    <li class="gap-1 d-flex align-items-center mb-2">
                                        <i class="ti ti-columns me-1"></i>
                                        <div class="form-check form-switch w-100 ps-0">
                                            <label class="form-check-label d-flex align-items-center gap-2 w-100">
                                                <span>Slug</span>
                                                <input class="form-check-input column-toggle ms-auto" type="checkbox" data-column="2" checked>
                                            </label>
                                        </div>
                                    </li>

                                    <!-- Order Column -->
                                    <li class="gap-1 d-flex align-items-center mb-2">
                                        <i class="ti ti-columns me-1"></i>
                                        <div class="form-check form-switch w-100 ps-0">
                                            <label class="form-check-label d-flex align-items-center gap-2 w-100">
                                                <span>Order</span>
                                                <input class="form-check-input column-toggle ms-auto" type="checkbox" data-column="3" checked>
                                            </label>
                                        </div>
                                    </li>

                                    <!-- Status Column -->
                                    <li class="gap-1 d-flex align-items-center mb-0">
                                        <i class="ti ti-columns me-1"></i>
                                        <div class="form-check form-switch w-100 ps-0">
                                            <label class="form-check-label d-flex align-items-center gap-2 w-100">
                                                <span>Status</span>
                                                <input class="form-check-input column-toggle ms-auto" type="checkbox" data-column="4" checked>
                                            </label>
                                        </div>
                                    </li>

                                </ul>
                            </div>
                        </div>

                    </div>

                </div>

                <!-- =========================
                     BLOG CATEGORY TABLE
                ========================== -->

                <div class="table-responsive custom-table">
                    <table class="table table-nowrap" id="categories-table">
                        <thead class="table-light">
                            <tr>
                                <th>#</th>
                                <th>Category Name</th>
                                <th>Slug</th>
                                <th>Order</th>
                                <!-- <th>Status</th> -->
                                <th class="text-end no-sort">Action</th>
                            </tr>
                        </thead>

                        <tbody id="categoriesTableBody">
                            <% if (categories && categories.length) { %>
                                <% categories.forEach(function(category, index) { %>
                                    <tr data-status="<%= category.status ? 'active' : 'inactive' %>">

                                        <!-- ID -->
                                        <td><%= category.id %></td>

                                        <!-- NAME -->
                                        <td>
                                            <h6 class="mb-0 fw-semibold">
                                                <a href="/admin/blog-categories/<%= category.id %>/edit" class="text-dark">
                                                    <%= category.name %>
                                                </a>
                                            </h6>
                                        </td>

                                        <!-- SLUG -->
                                        <td>
                                            <span class="badge bg-soft-primary text-primary">
                                                <i class="ti ti-link me-1"></i>
                                                <%= category.slug %>
                                            </span>
                                        </td>

                                        <!-- ORDER -->
                                        <td>
                                            <span class="text-muted">
                                                <%= category.ordering ?? 0 %>
                                            </span>
                                        </td>

                                        <!-- STATUS -->
                                        <!-- <td>
                                            <% if (category.status) { %>
                                                <span class="badge bg-soft-success text-success">
                                                    <i class="ti ti-circle-filled fs-7 me-1"></i> Active
                                                </span>
                                            <% } else { %>
                                                <span class="badge bg-soft-danger text-danger">
                                                    <i class="ti ti-circle-filled fs-7 me-1"></i> Inactive
                                                </span>
                                            <% } %>
                                        </td> -->

                                        <!-- ACTION -->
                                        <td class="text-end">
                                            <div class="dropdown">
                                                <a href="javascript:void(0);" class="btn btn-icon btn-sm" data-bs-toggle="dropdown" aria-expanded="false">
                                                    <i class="ti ti-dots-vertical"></i>
                                                </a>

                                                <ul class="dropdown-menu dropdown-menu-end">
                                                    <!-- Edit -->
                                                    <li>
                                                        <a href="/admin/blog-categories/<%= category.id %>/edit" class="dropdown-item">
                                                            <i class="ti ti-edit me-1"></i> Edit
                                                        </a>
                                                    </li>
                                                    <!-- Delete -->
                                                    <li>
                                                        <a href="javascript:void(0);" class="dropdown-item text-danger" data-bs-toggle="modal" data-bs-target="#delete_category_<%= category.id %>">
                                                            <i class="ti ti-trash me-1"></i> Delete
                                                        </a>
                                                    </li>
                                                </ul>
                                            </div>
                                        </td>

                                    </tr>
                                <% }) %>
                            <% } else { %>
                                <!-- EMPTY -->
                                <tr id="noCategoriesRow">
                                    <td colspan="6" class="text-center py-5">
                                        <div class="text-muted">
                                            <i class="ti ti-category fs-40 d-block mb-2"></i>
                                            <h5>No Categories Found</h5>
                                            <p class="mb-2">There are no blog categories available.</p>
                                            <a href="/admin/blog-categories/create" class="btn btn-primary btn-sm">
                                                <i class="ti ti-plus me-1"></i> Add Your First Category
                                            </a>
                                        </div>
                                    </td>
                                </tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>

                <!-- =========================
                     PAGINATION / COUNT
                ========================== -->

                <% if (typeof pagination !== 'undefined' && pagination && pagination.totalPages > 1) { %>
                    <div class="row align-items-center mt-3">

                        <!-- Info -->
                        <div class="col-md-6">
                            <div class="text-muted">
                                Page <%= pagination.page %> of <%= pagination.totalPages %> &nbsp;·&nbsp; <%= typeof total !== 'undefined' ? total : (categories ? categories.length : 0) %> total Categories
                            </div>
                        </div>

                        <!-- Pagination -->
                        <div class="col-md-6">
                            <div class="d-flex justify-content-end">
                                <ul class="pagination mb-0">

                                    <!-- Previous -->
                                    <% if (pagination.hasPrev) { %>
                                        <li class="page-item">
                                            <a class="page-link" href="/admin/blog-categories?page=<%= pagination.page - 1 %>&search=<%= encodeURIComponent(typeof search !== 'undefined' ? search : '') %>">
                                                <i class="ti ti-chevron-left"></i>
                                            </a>
                                        </li>
                                    <% } %>

                                    <!-- Pages -->
                                    <% if (pagination.pages && pagination.pages.length) { %>
                                        <% pagination.pages.forEach(function(page) { %>
                                            <li class="page-item <%= page === pagination.page ? 'active' : '' %>">
                                                <a class="page-link" href="/admin/blog-categories?page=<%= page %>&search=<%= encodeURIComponent(typeof search !== 'undefined' ? search : '') %>">
                                                    <%= page %>
                                                </a>
                                            </li>
                                        <% }) %>
                                    <% } %>

                                    <!-- Next -->
                                    <% if (pagination.hasNext) { %>
                                        <li class="page-item">
                                            <a class="page-link" href="/admin/blog-categories?page=<%= pagination.page + 1 %>&search=<%= encodeURIComponent(typeof search !== 'undefined' ? search : '') %>">
                                                <i class="ti ti-chevron-right"></i>
                                            </a>
                                        </li>
                                    <% } %>

                                </ul>
                            </div>
                        </div>

                    </div>
                <% } %>

            </div>

        </div>

    </div>

</div>

<!-- Manage Columns Toggle Script -->
<script>
    document.addEventListener('DOMContentLoaded', function () {
        const toggles = document.querySelectorAll('.column-toggle');
        const table = document.getElementById('categories-table');

        if (!table) return;

        function updateColumnVisibility(colIndex, isVisible) {
            // Header cell hide/show
            const th = table.querySelector(`thead tr th:nth-child(${colIndex + 1})`);
            if (th) {
                th.classList.toggle('d-none', !isVisible);
            }

            // Body cells hide/show
            const rows = table.querySelectorAll('tbody tr');
            rows.forEach(row => {
                // Ignore "No Categories Found" full width row
                if (row.id === 'noCategoriesRow') return;

                const td = row.querySelector(`td:nth-child(${colIndex + 1})`);
                if (td) {
                    td.classList.toggle('d-none', !isVisible);
                }
            });

            // Update empty row colspan
            const noCategoriesTd = table.querySelector('#noCategoriesRow td');
            if (noCategoriesTd) {
                const visibleThs = table.querySelectorAll('thead tr th:not(.d-none)').length;
                noCategoriesTd.setAttribute('colspan', visibleThs || 1);
            }
        }

        // Initialize toggle listeners & load saved preferences
        toggles.forEach(toggle => {
            const colIndex = parseInt(toggle.getAttribute('data-column'), 10);
            const storageKey = 'blog_cat_col_' + colIndex;

            // Restore from localStorage if saved
            const savedState = localStorage.getItem(storageKey);
            if (savedState !== null) {
                const isChecked = savedState === 'true';
                toggle.checked = isChecked;
                updateColumnVisibility(colIndex, isChecked);
            }

            // Event listener for toggle click
            toggle.addEventListener('change', function () {
                const isChecked = this.checked;
                updateColumnVisibility(colIndex, isChecked);
                localStorage.setItem(storageKey, isChecked);
            });
        });
    });
</script>

<%- include('../../adminlayout/footer') %>